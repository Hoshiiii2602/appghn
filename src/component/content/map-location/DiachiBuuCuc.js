import axios from 'axios'
import React from 'react'
import { useSelector } from 'react-redux'

export default function DiachiBuuCuc() {

        // var tokens = '5ba27886-599a-11ed-bd1f-1a28f04fff2f'
        // var baseUrl = `https://dev-online-gateway.ghn.vn/shiip/public-api/v2/station/get`
        // axios.get(baseUrl, {
        //     headers: {
        //         "Authorization": `token:${tokens}`
        //     }
        // }).then(function (result) { console.log(result.data) })
    return (
        <div className=''>DiachiBuuCuc</div>
    )
}
